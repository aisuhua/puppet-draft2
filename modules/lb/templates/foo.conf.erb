server {
    listen 80;
    listen [::]:80;
    server_name foo.aisuhua.com;

    add_header X-LB-Name <%= scope.call_function('lookup', ['idc::name']) %>_<%= @hostname %> always;

    location / {
        proxy_pass http://foo;
    }
}