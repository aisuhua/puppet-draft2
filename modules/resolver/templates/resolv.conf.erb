# Resolv.conf generated by Puppet

<% [scope.call_function('lookup', ['resolver::nameservers'])].flatten.each do |ns| -%>
nameserver <%= ns %>
<% end -%>

# Other values can be added or hard-coded into the template as needed.
